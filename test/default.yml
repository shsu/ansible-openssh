---
- hosts: test-kitchen
  remote_user: root

  roles:
    - role: ansible-openssh
      openssh_client: yes
      openssh_server: yes
      openssh_ssh_host:
        rsa:
        application:
        development:
        public: | REDACT
        private:| REDACT
      project: travis
      enviroment_tier: travis
      domain: travis
