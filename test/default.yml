---
- hosts: test-kitchen
  remote_user: root

  roles:
    - role: ansible-openssh
      openssh_client: yes
      openssh_server: yes
      openssh_host_keys:
        - /etc/ssh/ssh_host_rsa_key
      project: travis
      enviroment_tier: travis
      domain: travis
